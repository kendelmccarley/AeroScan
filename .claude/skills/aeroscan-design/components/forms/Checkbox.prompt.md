One-sentence: a square instrument checkbox with checked / unchecked / indeterminate states — amber fill + dark glyph when on, teal outline when off.

```jsx
<Checkbox checked={v} onChange={setV} label="12-hour clock" />
<Checkbox indeterminate />
```

Tri-state mirrors the firmware's checkbox_indeterminate glyph.
