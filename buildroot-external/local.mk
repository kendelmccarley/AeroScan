# Local source overrides for development.
# Uncomment and set the path when iterating on aeroscan-gui without
# re-downloading the source tarball on every build.
#
AEROSCAN_GUI_OVERRIDE_SRCDIR = $(TOPDIR)/../aeroscan-gui
