ctrl_interface=/var/run/wpa_supplicant
ctrl_interface_group=0
update_config=1
country=US

# Networks are added via Settings → WiFi in the application,
# or manually:
#
# network={
#     ssid="MyNetwork"
#     psk="password"
# }
