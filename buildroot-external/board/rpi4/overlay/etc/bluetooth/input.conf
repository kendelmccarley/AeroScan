[General]
# HID idle timeout in minutes; 0 = never disconnect on idle.
IdleTimeout=0

# Keyboard pages the Pi when it powers on; no host-initiated scan needed.
# Setting true causes bluetoothd to hold StartDiscovery on every boot (InProgress).
Reconnect=false
