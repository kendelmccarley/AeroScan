# Cap the persistent journal so logging can never eat meaningful SD space
# (the 10%-of-filesystem default is ~2.9 GB on an expanded 32 GB card).
# 64 MB holds several days of this system's normal logging volume.
[Journal]
SystemMaxUse=64M
