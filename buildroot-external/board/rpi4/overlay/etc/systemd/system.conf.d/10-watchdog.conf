# Feed the BCM2711 hardware watchdog: if the kernel or PID 1 hard-hangs,
# the SoC reboots the box after 15 s instead of requiring a power cycle.
# Part of the run-indefinitely appliance posture.
[Manager]
RuntimeWatchdogSec=15
