# Onboard BCM43455 WiFi (RPi4) via brcmfmac.
# Loaded at sysinit so wlan0 exists before wpa_supplicant@wlan0.service starts.
# modprobe resolves the full dep chain: rfkill → cfg80211 → brcmutil → brcmfmac.
# The RT5370 USB dongle (if present) is handled by udev MODALIAS separately.
brcmfmac
