# VC4 KMS display driver — loaded early so the framebuffer console on tty1
# is available before userspace services start.  CONFIG_DRM_VC4 is a module
# (not built-in) because it depends on SND_SOC which is also a module in
# bcm2711_defconfig; explicit load here avoids relying on udev MODALIAS timing.
vc4
